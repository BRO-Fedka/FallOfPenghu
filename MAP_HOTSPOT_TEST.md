# Требования к тесту горячих точек карты

Для второго агента, который готовит геометрию. Цель: скользящим прямоугольником экрана найти места, где CPU-вектор pygame-ce получит больше всего *вызовов отрисовки* и вершин, на разных размерах окна и разных LOD.

Игра рисует только вектор. Растровые тайлы не рассматриваются. Розовых дорог нет и в тесте их быть не должно.

Машина разработки, для справки: ASUS ROG Strix G614JU, i7-13650HX, 16 ГБ. Калибровать отчёт не под неё одну: нужны абсолютные счётчики, не «на этом ноутбуке потянет».

Замер Тайваня уже есть и в этот проход не входит как цель: 717 вершин, 1 полигон, 47.6×191.0 км, 298.4 м/px, draw 0.88 ms. Новый тест ищет худшие вьюпорты по Пэнху и по всей игровой рамке.

---

## 1. Система координат и прямоугольник экрана

- CRS: EPSG:3825, единицы — метры.
- Ось Y как в данных карты. В отчёте явно написать, растёт ли Y на север.
- Прямоугольник экрана задаётся в метрах: центр `(cx, cy)`, ширина `W`, высота `H`.
- `W = pixel_width × meters_per_pixel`
- `H = pixel_height × meters_per_pixel`
- Не подменять экран квадратом «диаметр камеры», если окно 16:9: это широкий прямоугольник.

Шаг скольжения:

- По X и Y шаг = 25% размера прямоугольника (перекрытие 75%).
- Дополнительно прогнать те же размеры, прижатые к bbox каждого крупного острова Пэнху (Магун / Magong, Baisha, Xiyu, и прочие обитаемые), чтобы не промахнуться мимо города из‑за сетки.
- Не сканировать пустое море дальше 20 км от ближайшей суши, кроме одного контрольного прохода на масштабе с Тайванем в кадре.

---

## 2. Матрица тестов: окно × LOD

Прогнать полное произведение. Не усреднять по островам: для каждой ячейки матрицы вернуть свои топ-горячие точки.

### Размеры окна в пикселях

| Код | Ширина × высота | Зачем |
|---|---|---|
| S | 1280 × 720 | нижняя граница играбельного окна |
| M | 1920 × 1080 | основной |
| L | 2560 × 1440 | плотнее пикселей, больше объектов проходит порог «> 1 px» |

Если 2560×1440 слишком дорого считать, L можно заменить на 1920×1080 с тем же м/px, что у 2560×1440 на том же диаметре (то есть более крупный мир-прямоугольник). Это указать в отчёте.

### LOD / масштаб мира

Масштаб задавать **диаметром по ширине экрана** (сколько метров по горизонтали видно), не «диаметром круга камеры». Тогда `meters_per_pixel = world_width / pixel_width`.

| Код LOD | Видимая ширина | Примерно м/px на 1920×1080 | Что существует для отрисовки |
|---|---:|---:|---|
| L0 | 180 км | 93.8 | Тайвань + упрощённый берег крупных островов Пэнху. Зелень нет или только пятна ≥ 0.5 км². Дорог нет. Зданий нет. Аэропорт — не более силуэта, если контур ≥ 4 px. |
| L1 | 80 км | 41.7 | Берег ε≈80–150 м. Зелень — только крупные пятна. Дорог нет, кроме мостов как 1 линия, если длина ≥ 4 px. Зданий нет. |
| L2 | 40 км | 20.8 | Берег ε≈40–80 м. Зелень упрощённая, без мелких дыр. Магистрали и мосты. Зданий нет. |
| L3 | 15 км | 7.8 | Берег ε≈20–40 м. Зелень в кадре. Дороги тонкой линией, без ширины. Зданий нет. Отдельного слоя стратегических зданий в карте нет. |
| L4 | 8 км | 4.2 | Берег ε≈8–20 м. Зелень в кадре. Дороги в кадре. Зданий нет. |
| L5 | 2 км | 1.04 | Полный берег видимых кусков. Зелень. Дороги, ширина только у мостов. Здания **ещё нет**. |
| L6 | 1 км | 0.52 | Как L5, плюс здания. |
| L7 | 300 м | 0.156 | Как L6, полная детализация видимого. Дороги с шириной, если ширина ≥ 1.5 px. |

Если упрощённые контуры (ε) ещё не посчитаны, для L0–L4 всё равно отфильтровать *набор объектов* по правилам выше, а вершины считать по той геометрии, что есть. В отчёте пометить: `lod_geometry: full | simplified`.

Порог появления обычных зданий: **L6 и L7**. На L5 и грубее обычные дома в сложность отрисовки не входят.

---

## 3. Единица сложности

Считать не «объект ГИС», а **предполагаемый вызов pygame**:

| Тип | 1 единица draw | Вершины |
|---|---|---|
| Полигон заливки (остров, зелень, здание, перрон) | 1, даже если много дыр, *если* рисуется одним `polygon` | сумма внешнего кольца + дыр |
| Полилиния (дорога, рулёжка) | 1 на всю линию, не на каждое ребро | число точек линии |
| Мост | 1 | точки его геометрии |
| Прямоугольное здание | 1 (`rect` или 4-точечный polygon) | 4 |

Для каждой горячей точки вернуть:

- `draw_calls` — сумма единиц выше, пересекающих прямоугольник
- `vertices` — сумма вершин этих единиц
- разбивку по слоям: берег, зелень, OSM-здания, смоделированные здания, дороги, аэропорты, Тайвань
- `fill_km2` — площадь пересечения заливок с прямоугольником экрана (км²). Это оценка стоимости scanline, не замена счётчику вызовов

### Два режима пересечения

Без нарезки полигонов pygame зальёт *весь* полигон, если он хоть чуть попал в кадр.

Поэтому для каждого окна считать два столбика:

1. `intersect_whole` — объект целиком, если bbox/контур пересекает экран. Это текущий худший случай.
2. `clipped_or_chunked` — если полигон разрезать сеткой 1 км × 1 км, сколько *кусков* и вершин реально попало бы в экран. Если нарезки в данных нет, посчитать аналитически: площадь/вершины пересечения с прямоугольником и оценка числа кусков = сколько клеток 1 км сетки пересекают и полигон, и экран.

Оба числа нужны. Если `intersect_whole.vertices` >> `clipped_or_chunked.vertices`, нарезка берега/зелени обязательна.

---

## 4. Что **не** учитывать в сложности отрисовки

Не включать в `draw_calls` и `vertices`:

1. Розовые / неучтённые дороги и любой служебный слой, которого не будет в игре.
2. 415 пиксельных зон и 4 контура аэродромов, которые уже решено не рисовать.
3. Карту высот / DEM / любой растр.
4. Атрибуты, подписи, теги организаций, внутренние идентификаторы, топологические узлы без геометрии отрисовки.
5. Объекты, которые на данном LOD выключены (все обычные здания на L0–L5; зелень на L0, если не прошла порог площади; дороги на L0).
6. Объекты, чей bbox после проекции в пиксели меньше **1.0 px** по большей стороне.
7. Полигоны с площадью на экране меньше **2 px²**.
8. Участки линии, которые после проекции короче **2 px** целиком (вся полилиния целиком короче 2 px — выкинуть; длинную дорогу оставить и считать целиком, не резать на пиксельные сегменты в этом тесте).
9. Повторную геометрию: если одно и то же здание есть в OSM и в смоделированном слое, в нагрузке оставить **один** экземпляр. Правило приоритета: **OSM остаётся, модель выбрасывается**. В отчёте написать, сколько дублей отброшено.
10. Тайвань, если ни один его пиксель не попадает в прямоугольник. Не держать его «всегда включённым» на вьюпорте Пэнху.
11. Дыры полигона как отдельные объекты, если они рисуются как holes одного `draw.polygon`.
12. Будущие игровые сущности (дроны, ПВО, машины, фронт, подписи UI). Этот тест только статика карты.
13. Сглаживание (`aalines` / второй проход gfxdraw) не умножать: в игре его не будет на всю карту.

Море-фон не объект. `fill` окна не входит в `draw_calls`.

---

## 5. Что учитывать обязательно

- Берег Пэнху.
- Тайвань — только если пересекает текущий прямоугольник.
- Растительность, которая осталась после сокращения и прошла LOD/площадь/пиксельный порог. Если слой ещё режется, прогнать тест по **текущей** зелени и пометить `vegetation_status: in_progress` плюс дату/хеш данных.
- Учтённые дороги (1475 объектов — точное число), прошедшие LOD и пиксельный порог.
- OSM-здания и смоделированные здания — только L6 и L7, без дублей.
- Аэропортовые полигоны/линии, которые рисуются (ВПП, перрон, рулёжки, стоянки), если прошли пиксельный порог.
- Мосты как отдельные draw, даже если на грубом LOD прочие дороги выключены (L1+).

---

## 6. Что вернуть в отчёте

Формат: один машиночитаемый JSON плюс короткий человеческий markdown. JSON — главный.

Для каждой ячейки матрицы `(window, lod)`:

```text
window, lod, m_per_px,
top: [
  {
    rank,
    cx, cy,          // м, EPSG:3825
    world_w, world_h,
    island_hint,     // имя острова, если известно
    intersect_whole:  { draw_calls, vertices, fill_km2, by_layer: {...} },
    clipped_or_chunked: { draw_calls, vertices, fill_km2, chunks, by_layer: {...} }
  },
  ...  // ровно топ-5
]
```

Дополнительно по всей матрице:

- абсолютный чемпион по `intersect_whole.draw_calls`;
- абсолютный чемпион по `intersect_whole.vertices`;
- абсолютный чемпион по `clipped_or_chunked.draw_calls` на L6 и L7 (ожидаемо центр Магуна);
- список ячеек, где `intersect_whole.draw_calls` ≥ 800;
- список ячеек, где `intersect_whole.vertices` ≥ 20 000;
- число отброшенных дублей зданий;
- хеш или имя файла карты, версия зелени.

Не присылать скриншоты вместо чисел. Скриншот топ-1 на L6/M можно как иллюстрацию, не вместо JSON.

Не усреднять FPS и не рисовать кадр, если это замедлит проход. Этот тест — счётчик пересечений, не бенчмарк `draw`. Отдельный draw-бенчмарк можно сделать потом только для топ-1 каждой LOD на окне 1920×1080.

---

## 7. Пороги «может перегрузить»

Помечать hotspot как `risk: high`, если выполняется любое:

- `intersect_whole.draw_calls` ≥ 800
- `clipped_or_chunked.draw_calls` ≥ 800
- `intersect_whole.vertices` ≥ 20 000 при `draw_calls` ≥ 50 (один огромный ненарезанный полигон)
- на L6/L7 `clipped_or_chunked` зданий ≥ 600

`risk: medium` при 400–799 вызовов после clip/chunk.

Эти пороги — для отчёта, не приказ выкидывать геометрию. Решение по зелени и LOD остаётся за автором карты.

---

## 8. Чего не делать

- Не сканировать шаг в 1 м.
- Не включать розовый слой «на всякий случай».
- Не считать каждое ребро дороги отдельным объектом.
- Не считать вершины объектов вне прямоугольника, кроме режима `intersect_whole`.
- Не подменять LOD «все слои сразу на всех масштабах» — это бесполезно для игры.
- Не оптимизировать геометрию молча по ходу теста: сначала отчёт по текущим данным.

---

## 9. Поправка после первого прогона (2026-08-30)

Отчёт снят: `output/map_hotspot_report.md` и исходный JSON агента карты. Расхождения с этим ТЗ и с игрой:

- Здания в прогоне считались с L3. В игре обычные дома включаются с L6. Чемпион 4273 вызовов на L/L4 Magong — в основном ~3734 дома в окне 8×4.5 км, не нагрузка «игрового» L4.
- `clipped_or_chunked` на L0 раздут нарезкой Тайваня сеткой 1 км (~2889 кусков на 1 полигон). Такой клип в игру не класть. Сетка 1 км — аналитика, не меш.
- Пересечения зданий: OSM побеждает, модель выбрасывается. Дублей 0.
- Стратегических зданий в данных нет; все дома равнозначны.
- Дороги: 1476, мосты внутри `roads`.
- Зелень зафиксирована, variant F, хеш `9fa777810c3a`.
- Карта хранит одну полную геометрию. ε берега в отчёте — только для счётчика.
- На ближнем LOD самый плотный квартал — Qimei (~515 домов), не Magong. Порог 600 не взят.
