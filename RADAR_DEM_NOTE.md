# Пояснительная записка: поставка `dem.npz`

Для агента радарного режима / рантайма. Читать вместе с `RADAR_DEM_SPEC.md`. Карта высот сдана; **вторую поставку с меньшим шагом не ждать**.

---

## Решение хозяина

Клетка **30 м** принята как основной слой.

Пункт спецификации «не принимать ≥ 30 м как основной слой» **к этой поставке не применяется**. Хозяин дал согласие. Большей точности из этой среды достичь нельзя: официальный DTM Тайваня (MOI / NLSC, 20 м и лучше) с TGOS не скачивается (ответ 403). Доступный источник — Copernicus DEM GLO-30, он же был в спецификации вторым в рейтинге.

Не браковать пакет из‑за `cell_m = 30`. Не запрашивать повторную нарезку в 10–20 м без нового источника.

---

## Что лежит в пакете

| Файл | Для кого |
|---|---|
| `penghu_map_v1/dem.npz` | игра, радар |
| `penghu_map_v1/dem_src.tif` | проверка в GIS; игра **не** читает |

Ключ `layers.dem` есть в `manifest.json`. Из `excluded` слой `dem` убран. Слои `piers` и `gl_fields` не менялись.

`origin_epsg3825` тот же, что у остальной карты:

```text
[303629.30711733666, 2596980.7306913873]
```

Пересчитывать origin и `frame` не нужно.

---

## Формат (как в спецификации)

Ключи `dem.npz` (`numpy.load`):

| Ключ | Тип | Смысл |
|---|---|---|
| `height` | float32 `(2354, 1537)` | метры над уровнем моря; строка 0 = юг, столбец 0 = запад |
| `frame` | float64 `[minx, miny, maxx, maxy]` | **локальные** метры, не абсолютный 3825 |
| `cell_m` | float64 `30` | квадратная клетка |
| `nodata` | float32 `-1000` | океан и лагуны вне `coast.geojson` |
| `vertical` | unicode | `EGM2008` |
| `source` | unicode | Copernicus GLO-30 + маска берега |

Покрытие: `bbox_penghu` плюс пад 2 км (`coverage: penghu_pad_2km`). Тайвань в растр не входит.

Океан — **nodata**, не `0`. Ноль на воде даст ложную горизонталь 0 м вдоль берега; так не сделано.

Горизонтали в пакет **не** входят. Игра снимает изогипси с растра сама, шаг **5 м**.

---

## Приёмка (уже пройдена)

- Magong Harbor, локальные `(3766, 9102)`: внутри `coast.geojson`, высота конечная **≈ 0 м** (набережная), не nodata, не 400.
- Пиксель западнее западного берега: `-1000`.
- Суша по маске `coast.geojson`: 100 % пикселей с высотой; диапазон примерно −3…65 м (архипелаг низкий, это норма).
- `cell_m` согласован с `frame` и размером лучше 1 %.

Не поднимать сушу искусственно. Не сглаживать растр в рантайме.

---

## Как читать в игре

1. Нет файла / нет `layers.dem` → горизонталей нет (старое поведение).
2. Файл есть → маска `height == nodata` (канон `-1000`), по суше — изолинии 5 м.
3. UV: `uv.y = 0` у южного края, как у GL-текстуры; массив уже south-up.
4. Координаты сэмпла — локальные метры пакета, те же, что у geojson.
